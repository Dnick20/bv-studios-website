#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running code validation..."
npm run validate

if [ $? -ne 0 ]; then
  echo "❌ Code validation failed. Please fix the errors before committing."
  exit 1
fi

echo "✅ Code validation passed!"
